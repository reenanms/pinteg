{
    "name": "pinteg-monorepo",
    "private": true,
    "scripts": {
        "build": "npm run build --workspaces --if-present",
        "test": "npm run test --workspaces --if-present",
        "dev": "npm run dev --workspace @pinteg/demo-crud-react"
    },
    "workspaces": [
        "packages/*",
        "apps/*"
    ]
}